# Audiology
Developed an end-to-end ML pipeline that extracts clinically motivated audiometric features from a 992-sample, six-class audiogram dataset, applies SMOTE-balanced stratified cross-validation across seven classifiers, and uses SHAP explainability to identify which high-frequency thresholds (4–8 kHz) most strongly predict early-stage hearing loss.
